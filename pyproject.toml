[project]
name = "mateon-ai"
version = "0.1.0"
description = "AI server for Mate-On"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "openai>=1.55",
    "tenacity>=9.0",
    "python-multipart>=0.0.32",
    "pymupdf>=1.28.0",
    "supabase>=2.9",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.8",
    "streamlit>=1.38",
    # scripts/generate_contest_graph_visualization.py의 PCA 계산용 — 프로덕션 앱 코드에서는 쓰지 않는다.
    "numpy>=1.26",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "live: tests that call the real OpenAI API (excluded by default)",
]
addopts = "-m 'not live'"
